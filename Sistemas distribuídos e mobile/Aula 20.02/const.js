// declarando constantes

const nome = "Juan"
console.log(nome)

nome = "Pires"
// não pode reatribuir - ele só retorna o erro depois de executar (ou a expressão certa 'interpretar')
// diferente do Java que possui um compilador e não executa

const outroNome = 'Jonas'
const maisUmNome = `Jenifer` // pode utilizar "aspas duplas" ou 'aspas simples' ou `crase`

const idade = 17
idade.falar() // exemplo de uma função